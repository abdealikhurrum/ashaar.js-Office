# macOS metadata
.DS_Store
**/.DS_Store

# SSL dev certs (generated by npm run certs)
localhost.pem
localhost-key.pem
*.pem

# Dependencies
node_modules/

# New test documents (existing corpus fixtures are tracked)
#test-documents/*.docx

# Brainstorm scratch (visual companion)
.superpowers/

# Word lock / temp files
~$*

# Private/licensed fonts: never commit or deploy to the public GitHub Pages site.
# Jameel Noori Nastaleeq Kasheeda is redistributed under a license that does not
# permit public rehosting — drop it locally for testing only (see README.md).
assets/fonts/JameelNooriNastaleeqKasheeda*
