# macOS metadata
.DS_Store
**/.DS_Store

# SSL dev certs (generated by npm run certs)
localhost.pem
localhost-key.pem
*.pem

# Dependencies
node_modules/

# New test documents (existing corpus fixtures are tracked)
#test-documents/*.docx

# Brainstorm scratch (visual companion)
.superpowers/

# Word lock / temp files
~$*

# Private/licensed fonts: never commit or deploy to the public GitHub Pages site.
# Jameel Noori Nastaleeq (base) and its Kasheeda face are redistributed under a
# license that does not permit public rehosting — drop them locally for testing
# only (see README.md). font-swap mechanism uses both faces (fonts.js `jameel`).
assets/fonts/JameelNooriNastaleeq*

# local browser-demo tooling artifacts
.playwright-mcp/
